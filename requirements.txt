# cacheblend-hf-v7 — slim musique-only distribution.
# Versions locked to match the v4 reproduction environment.
#
# Install order on CPU (Mac for scaffolding test):
#   pip install torch==2.4.1
#   pip install -r requirements.txt
#   pip install -e .
#
# Install order on GPU pod (vast.ai pytorch:2.4.1-cuda12.4 image — torch already at correct version):
#   grep -v -E '^torch(\s|=|$)' requirements.txt > /tmp/reqs-no-torch.txt
#   pip install -r /tmp/reqs-no-torch.txt
#   pip install -e .

# --- Core (locked exactly) ---
torch==2.4.1
# transformers: run env uses 4.51.3 (KVzip imports qwen3, added in 4.51.0).
# Lower bound 4.51.0 (qwen3); upper bound <4.53 because this code uses the
# internal `_update_causal_mask` API, removed in 4.53+ (see CODE-REVIEW C2/API
# drift). The decoder-layer cache-kwarg rename is handled at runtime by
# cacheblend.model.call_decoder_layer, so both 4.51 (singular) and 5.x (plural)
# call correctly — but full 5.x support also needs the mask-API migration.
transformers>=4.51.0,<4.53
accelerate==1.13.0
huggingface-hub==0.36.2
tokenizers==0.21.4
safetensors==0.7.0

# --- Metrics for F1/ROUGE-L (musique original utils.py imports rouge_score) ---
rouge-score>=0.1.2

# --- numerics ---
numpy>=2.0,<3.0
