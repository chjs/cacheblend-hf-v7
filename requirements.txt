# cacheblend-hf-v7 — slim musique-only distribution.
# Versions locked to match the v4 reproduction environment.
#
# Install order on CPU (Mac for scaffolding test):
#   pip install torch==2.4.1
#   pip install -r requirements.txt
#   pip install -e .
#
# Install order on GPU pod (vast.ai pytorch:2.4.1-cuda12.4 image — torch already at correct version):
#   grep -v -E '^torch(\s|=|$)' requirements.txt > /tmp/reqs-no-torch.txt
#   pip install -r /tmp/reqs-no-torch.txt
#   pip install -e .

# --- Core (locked exactly) ---
torch==2.4.1
# transformers: PIN EXACTLY 4.51.3 — the only version validated end-to-end.
# (qwen3 needs >=4.51.0; >=4.52 changes post_init/tensor-parallel handling and
#  >=4.53 removes the internal _update_causal_mask API this code uses. A loose
#  range let pip pick 4.52.x, which breaks Llama load with ALL_PARALLEL_STYLES.)
transformers==4.51.3
accelerate==1.13.0
huggingface-hub==0.36.2
tokenizers==0.21.4
safetensors==0.7.0

# --- Metrics for F1/ROUGE-L (musique original utils.py imports rouge_score) ---
rouge-score>=0.1.2

# --- numerics ---
numpy>=2.0,<3.0
